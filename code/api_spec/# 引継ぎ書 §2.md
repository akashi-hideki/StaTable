# 引継ぎ書 §2.3 目次修正の正式承認版

選択肢1のご承認を受け、**修正後の目次を正式版として確定**します。以降のS5〜S8は本目次に準拠して進めます。

---

## 1. 修正後の正式目次（10章構成を維持）

```
StaTable SDK API Reference（改訂版）
├── 1. 概要（Overview）
├── 2. クイックスタート（Quick Start）
├── 3. データモデル（Core Data Model）★§3+§7統合
│   ├── 3.1 列挙型（Enums）
│   │      StateType / EventKind / EventDeliveryType / EventSourceLayer
│   ├── 3.2 状態・イベント
│   │      State / Event
│   ├── 3.3 遷移
│   │      Transition
│   ├── 3.4 アクション・関係（v2.2）
│   │      ActionStep / TransitionRelation
│   ├── 3.5 ロール関数
│   │      RoleFunction
│   ├── 3.6 StateMachine（コンテナ）
│   │      全17メソッド
│   ├── 3.7 GlobalDefinitions（コンテナ）
│   │      全4メソッド
│   └── 3.8 その他dataclass
│          StructMemberDef / CustomTypeDef / SystemVariable /
│          EventFlag / InterruptAction / InterruptHandlerDef /
│          DevicePlaceholderDef / TimerDerivedDef / TimerBaseDef /
│          EventQueueDef
├── 4. コード生成API（Code Generation API）
│   ├── 4.1 CCodeGenerator
│   ├── 4.2 CodeGenerationConfig
│   ├── 4.3 ConfigManager
│   ├── 4.4 RoleFunctionGenerator（サブジェネレータ）
│   ├── 4.5 TransitionGenerator（サブジェネレータ）
│   ├── 4.6 CodeTemplates（テンプレート辞書）
│   └── 4.7 内部サブジェネレータ（要判断）
│          StructGenerator / EnumGenerator / VariableGenerator /
│          EventQueueGenerator / InterruptGenerator /
│          TimerGenerator / OSALGenerator
├── 5. 検証API（Validation API）★新設
│   ├── 5.1 CodeGenerationValidator
│   ├── 5.2 Item Validators（10種）
│   │      StateValidator / EventValidator / TransitionValidator /
│   │      CellValidator / RoleFunctionValidator / FlagValidator /
│   │      VariableValidator / TimerValidator / QueueValidator /
│   │      InterruptValidator / CustomTypeValidator
│   ├── 5.3 AI診断連携
│   │      AIPromptGenerator / AIResponseParser
│   └── 5.4 変更適用
│          ChangeApplier / ChangeRequest / ChangeActionType
├── 6. MISRA連携API（MISRA Integration API）
│   ├── 6.1 MisraRunner（要確認）
│   └── 6.2 MisraReport（要確認）
├── 7. ユーティリティAPI（Utility API）
│   ├── 7.1 XmlIO（関数群）★クラスではなく関数
│   │      project_to_xml / project_from_xml /
│   │      state_machine_to/from_element /
│   │      global_defs_to/from_element 他
│   ├── 7.2 MermaidGenerator（関数）★
│   │      generate_mermaid
│   └── 7.3 SampleData（関数群）★
│          create_sample_state_machine / create_sample_global_defs
├── 8. エラーコード（Error Codes）
├── 9. 制限事項（Limitations）
└── 10. 付録（Appendix）
```

**章数の維持**：統合（§3+§7）と新設（§5）で**相殺**。元の「10章構成」を維持。

---

## 2. 変更内容の対照表

| 項目 | 旧（引継ぎ書 §2.3） | 新（承認版） | 理由 |
|------|-------------------|-------------|------|
| §3 コアAPI | `StateMachineBuilder` 等5クラス | `StateMachine` + dataclass群 | **実在しない**。dataclassベース |
| §3-§7 | §3（API）と§7（データモデル）が分離 | §3に統合 | **重複解消**。Builderが実在しないため分離の意味消失 |
| §4 コード生成API | `CCodeGenerator`, `CodeGenerationConfig`, `TemplateCustomizer` | 同 + サブジェネレータ4種 | `TemplateCustomizer` は実在せず `CodeTemplates` に置換。サブジェネレータを明示 |
| §4-§5 | MISRA連携が§5 | **§5 検証API を新設**、MISRAは§6へ | `codegen/validate/`（15+クラス）の存在（U-01） |
| §5 MISRA連携API | §5 | §6に繰り下げ | 上記の繰り下げ |
| §6 ユーティリティAPI | `XmlIO` / `MermaidGenerator` / `Validator` | 同（`Validator` は §5 に移動） | クラス/関数を明示。`Validator` は検証APIへ |
| §7 データモデル | §7 | **§3に統合** | 重複解消 |
| §8 エラーコード | §8 | §8（変更なし） | 章番号維持 |
| §9 制限事項 | §9 | §9（変更なし） | 章番号維持 |
| §10 付録 | §10 | §10（変更なし） | 章番号維持 |

---

## 3. 具体的な修正ポイント（7項目）

| # | 修正 | 影響範囲 |
|---|------|---------|
| M-01 | **Builder系5クラスを全削除** | §3 |
| M-02 | **dataclassベースのデータモデルを§3に統合** | §3（旧§3+§7） |
| M-03 | **`TemplateCustomizer` → `CodeTemplates`** | §4.6 |
| M-04 | **サブジェネレータを§4に明示**（`RoleFunctionGenerator`, `TransitionGenerator` 等） | §4.4〜4.7 |
| M-05 | **§5 検証API を新設** | §5（新規） |
| M-06 | **MISRA連携APIを§6に繰り下げ** | §6 |
| M-07 | **XmlIO / MermaidGenerator / SampleData を関数として明示** | §7 |

---

## 4. H2 中間引継ぎ文書への追記（差分）

H2 の以下のセクションを更新します。

### §4「重大な発見」→ 解決済み

```markdown
## 4. 引継ぎ書 §2.3 の目次修正【解決済み】

**承認日**：2026-09-20
**修正内容**：M-01〜M-07（本スレッド §1〜§3 参照）
**結果**：
- Builder系5クラス削除
- §3と§7を統合（データモデル章）
- §5 検証API を新設
- §6 MISRA連携API、§7 ユーティリティAPI に繰り下げ
- 10章構成を維持
**ステータス**：確定。以降のS5〜S8は本目次に準拠。
```

### §5「累積の未解決事項」の更新

| # | 事項 | ステータス |
|---|------|-----------|
| U-01 | `codegen/validate/` の章立て | **解決（§5として独立章）** |
| U-12 | 引継ぎ書 §2.3 の目次修正 | **解決（本承認）** |

---

## 5. 未確定のまま残る事項

| # | 事項 | 検討先 |
|---|------|--------|
| U-10 | サブジェネレータ（RoleFunctionGenerator等）の**SDK公開判断** | S5（§4.4〜4.7の扱い） |
| U-11 | `CodeTemplates` の**カスタマイズAPI**の設計 | S6（§4.6の拡張） |
| U-13 | **検証API（§5）のSDK公開範囲** | S5〜S8 |
| U-14 | MISRA連携API（§6）の実体 | S7 |

---

## 6. 次のアクション：S5のソース共有

目次が確定したため、S5に進みます。以下の7ファイルを共有してください。

| # | ファイル | 優先度 | 想定章 |
|---|---------|--------|--------|
| 1 | `codegen/struct_generator.py` | 高 | §4.7 |
| 2 | `codegen/enum_generator.py` | 高 | §4.7 |
| 3 | `codegen/variable_generator.py` | 高 | §4.7 |
| 4 | `codegen/interrupt_generator.py` | 中 | §4.7 |
| 5 | `codegen/timer_generator.py` | 中 | §4.7 |
| 6 | `codegen/event_queue_generator.py` | 中 | §4.7 |
| 7 | `codegen/osal_generator.py` | 中 | §4.7 |

**分割共有も可**（例：1〜3を前半、4〜7を後半）。

**S5で判断する事項**：
- U-10：§4.7「内部サブジェネレータ」を **公開API** とするか **内部実装扱い** とするか
- U-13：`codegen/validate/` のクラス群を §5 にどう配置するか

**S5完了後**、S6（`type_mapper.py`, `naming_convention.py`, `code_merger.py`）+ **H3中間引継ぎ文書**を作成します。