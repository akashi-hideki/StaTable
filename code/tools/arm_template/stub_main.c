/* Minimal main for link verification.
 * The framework's real entry points are provided by the user;
 * this stub only satisfies the linker's reference to `main`.
 */
int main(void)
{
    for (;;) {
    }
    return 0;
}