# 全体統合に向けた検討方針

今回新たに追加した **SymbolPickerWidget** / **GuardEditDialog**（および既存のActionEditDialog）を、  
StaTable本体へ統合するにあたり、**親ウィンドウ・状態遷移表・Mermaid・設定パネル** との関係を整理します。

---

## 1. 親ウィンドウ（MainWindow）での位置づけ

- **グローバル定義（GlobalDefinitions）** は、プロジェクト全体で共有するメタデータと位置づけます。
- `MainWindow` が唯一の実体を持ち、各タブ（StateMachineTab）やダイアログへ参照を渡します（既に実装済み）。
- メニューは **Edit → Global Definitions...** でアクセス可能（既存）。
- 将来的には、ツールバーやステータスバーに「グローバル定義」ボタンを追加しても良いが、必須ではない。

**推奨：**  
- メニュー配置は **Edit → Global Definitions...** を維持。
- 各編集ダイアログ（ActionEditDialog / GuardEditDialog）からも「グローバル定義を開く...」ボタンでアクセス可能（既に実装済み）。

---

## 2. 状態遷移表（MatrixTableWidget）との連携

状態遷移表のセルには、**従来どおり遷移タイトル（遷移先・条件・動作の概要）** を表示します。  
グローバル変数やイベントフラグの内容は、**セル自体には表示しません**。  
ただし、ツールチップや編集ダイアログで確認・編集できるようにします。

### 変更点（必要に応じて）
- セルのダブルクリック → 遷移編集ダイアログ
- 遷移編集ダイアログの「遷移条件」セルをダブルクリック → **GuardEditDialog** を開く
- 遷移編集ダイアログの「動作」セルをダブルクリック → **ActionEditDialog** を開く（既存）
- どちらのダイアログにも **SymbolPickerWidget** を組み込み、変数・フラグ・ロール関数戻り値を挿入可能にする

**推奨：**  
- 状態遷移表のセル表示は変更しない。
- 編集ダイアログ内で完結する。

---

## 3. Mermaid表示への影響

**変更なし。**  
Mermaidには以下のように表示します。

- イベント名＋遷移条件のみ表示
- 動作内容（アクション）は表示しない（既に反映済み）
- グローバル変数やイベントフラグの定義内容はMermaidには出さない

**補足：**  
将来、状態遷移図に「状態のentry/exit/do」や「遷移条件」を表示する場合は、  
ラベルを短くするなど、可読性を考慮して別途検討します。

---

## 4. 設定パネル（SettingsPanel）との関係

現在、設定パネルには **状態一覧 / イベント辞書 / ロール関数** の3タブがあります。  
グローバル変数・イベントフラグは、**プロジェクト全体で共有**するため、  
設定パネルに追加せず、**独立したダイアログ** として維持します。

**理由：**
- 設定パネルはタブごとにステートマシン固有の情報を管理
- グローバル定義は全タブ共通のため、タブ内に置くと混乱しやすい

**推奨：**  
- 設定パネルには追加しない
- `Edit → Global Definitions...` または編集ダイアログのボタンから開く

---

## 5. プロジェクトXMLへの保存・ロード

既に実装済みです。

- `GlobalDefinitions` をプロジェクトXMLに保存
- ロード時は `global_defs` を復元し、`MainWindow` に保持
- 各タブへは参照を引き渡す

**補足：**  
- ロード後、各タブのダイアログを開いた際に、最新の `global_defs` が使われるように、  
  `StateMachineTab` → `MatrixTableWidget` → `TransitionListDialog` → `ActionEditDialog` / `GuardEditDialog` へ正しく渡す必要があります（既に実装済み）。

---

## 6. 統合の優先順位とステップ

1. **GuardEditDialog を遷移編集ダイアログに統合**  
   - 「遷移条件」セルのダブルクリックで開く
   - シンボルピッカーから条件式に挿入可能に

2. **ActionEditDialog の SymbolPickerWidget 化**  
   - 既存の左側一覧を `SymbolPickerWidget` に置き換え（重複排除）

3. **動作編集ダイアログと条件編集ダイアログの相互連携**  
   - どちらからでもグローバル定義画面を開ける
   - 新しい変数・フラグを登録したら即時反映

4. **プロジェクトXML保存・ロードの最終確認**  
   - グローバル定義が保存・復元されるか
   - 既存プロジェクト（グローバル定義なし）の後方互換確認

5. **テスト**  
   - 単体テスト（`test_symbol_picker_guard.py`）を活用
   - 統合後にGUI全体で動作確認

---

## 7. 将来の拡張性

- **コード生成**：グローバル変数を構造体にまとめる、イベントフラグをビットフィールドに展開
- **ツールバー**：「グローバル定義」ボタンを追加してアクセス向上
- **ステータスバー**：現在のグローバル変数・フラグ数を表示
- **検索バー**：状態遷移表全体からグローバル変数・フラグを検索

---

## 8. まとめ

| 項目 | 方針 |
|---|---|
| 親ウィンドウ | メニュー `Edit → Global Definitions...` でアクセス |
| 状態遷移表 | セル表示は変更なし。編集ダイアログで条件・動作を編集 |
| Mermaid | グローバル定義・動作内容は表示しない |
| 設定パネル | グローバル定義は追加しない |
| プロジェクトXML | 保存・ロード済み |
| 統合手順 | GuardEditDialog と SymbolPickerWidget を遷移編集に組み込む |

---

この方針で、既存のGUIデザインを崩さずに新機能を統合できます。  
ご意見があればお聞かせください。  
次に、実際の統合コーディングに進む場合は、その旨お知らせください。
